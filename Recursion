/**Пятое домашнее задание в рамках курса Java для  начинающих.
 * Рекурсия...
 */
public class Recursion {
    /** Объявляем метод для расчета суммы всех чисел до заданного нами числа
     * @param a передаем значение числа
     * @return сумму всех чисел до заданного
     */
    int PrintRecursoin (int a) {
        /** в sum сохрвним результат
         *
         */
    int sum=0;
        /**Когда наш метод рекурсивно "разложившись" дойдет до 0
         * нам необходимо выйти из рекурсии, во избежание зацикливания.
         * Мы вернем 0 так как происходит оперция сложения
         * и нам для корректного результата нужен именно 0
         * чтобы последняя операция сложения была a + 0
         * и рекурсия вышла из цикла 
         */
        if(a==0) {
            return 0; }
        /** до тех пор пока наш параметр не достиг значения 0
         * мы суммируем все числа по убывающей с шагом 1
         */
        else
             sum = (a+PrintRecursoin(a-1));
             return sum;


    }
}
